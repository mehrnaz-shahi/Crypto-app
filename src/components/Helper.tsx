import { Link } from "react-router-dom";


const Helper = () => {
    return (
        <div>

            <Link to="/home"> Home page</Link>
            
        </div>
    );
};

export default Helper;